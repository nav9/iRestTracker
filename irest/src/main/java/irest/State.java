package irest;

public class State {
    public LogManager log = new LogManager();
    public void run() {
        //dummy function that will be overridden in child classes. Could convert to interface if more complex functionality is required
    }
}